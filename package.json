{
  "private": true,
  "name": "test-app-pra",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^6.0.1",
    "eslint-config-synacor": "^3.0.4",
    "preact-cli": "^3.0.0",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "@arcteryx/components-modal": "^2.0.4",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2",
    "react-imgix": "^9.0.2",
    "styled-components": "^5.2.0",
    "styled-components-breakpoint": "^3.0.0-preview.20"
  }
}
